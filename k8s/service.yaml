apiVersion: v1
kind: Service
metadata:
  name: swiggy-frontend-service

spec:
  type: LoadBalancer

  selector:
    app: swiggy-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80