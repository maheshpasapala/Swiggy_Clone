apiVersion: v1
kind: Service
metadata:
  name: swiggy-clone-service
spec:
  type: NodePort
  selector:
    app: swiggy-clone
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080